<form method="post" action="{{ path('admin_trick_delete', {'id': trick.id}) }}" onsubmit="return confirm('{{ 'Voulez-vous vraiment supprimer cette figure ?' }}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trick.id) }}">
    <button type="submit" id="trick-delete" class="btn text-white"><i class="fa-solid fa-trash"></i></button>
</form>